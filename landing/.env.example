# Canonical URL for metadata / Open Graph (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Production: Vercel Blob read/write token (Storage → Blob in the Vercel project)
# Without this, waitlist emails append to landing/.data/waitlist.jsonl locally.
# BLOB_READ_WRITE_TOKEN=
